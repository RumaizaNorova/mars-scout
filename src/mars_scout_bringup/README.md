## `mars_scout_bringup`

Bringup package placeholder.

This package will own:

- Launch files that start the full stack (sim bridge + perception + geometry + control + logging)
- YAML configs (topic names, thresholds, max speed, frame names)

