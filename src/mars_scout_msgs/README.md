## `mars_scout_msgs`

Message package placeholder.

Planned messages:

- `GoalHypothesis.msg`: a grounded 2D hypothesis (ROI or point) plus confidence + metadata.

We will add actual ROS 2 interface files once we pin the ROS distro and build tooling.

